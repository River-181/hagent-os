[project]
name = "keg-workspace-tools"
version = "0.1.0"
description = "Portable tool stack for the KEG contest workspace"
requires-python = ">=3.11"
dependencies = [
  "notebooklm-mcp-cli==0.5.5",
]

[tool.uv]
package = false
