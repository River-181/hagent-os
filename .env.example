# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# Database (leave empty for embedded-postgres auto-start)
DATABASE_URL=

# Server
PORT=3100

# Deployment mode: local_trusted | authenticated
DEPLOYMENT_MODE=local_trusted
