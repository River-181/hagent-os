# 심사위원 가이드 — HagentOS 데모

> 이 문서는 심사위원이 2분 내에 HagentOS의 핵심 가치를 체험할 수 있도록 작성되었습니다.

---

## 1. 라이브 데모 접속

> **라이브 URL**: 준비 중 — 심사 전 업데이트 예정

라이브 URL 접속 시 별도 로그인 없이 **탄자니아 영어학원 데모 7** 조직이 사전 로드됩니다.

---

## 2. 로컬 실행 (API 키 불필요)

```bash
git clone https://github.com/River-181/hagent-os
cd hagent-os

# DEMO_MODE: AI API 키 없이 실제와 유사한 mock 응답 제공
echo "DEMO_MODE=true" > .env
echo "PORT=3200" >> .env

pnpm install && pnpm build

# 터미널 1: 서버
cd server && node dist/index.js

# 터미널 2: UI
cd ui && npx vite preview --port 5173
```

브라우저: `http://localhost:5173`

---

## 3. 2분 데모 시나리오

### Scene 1 — 대시보드 (20초)

`/{org}/dashboard`

- 이번 달 케이스 현황, 이탈 위험 학생, 에이전트 활동 요약 확인
- **포인트**: 원장이 앱을 열면 학원 상황이 한눈에 보임

### Scene 2 — AI가 처리한 케이스 (40초)

`/{org}/cases`

- **"이수아 무단결석 3회 연속"** 케이스 클릭
- AI 응답(agentDraft) 확인: "3주 연속 결석 패턴, 학부모 상담 권고, 금요일 오후 2시 예약"
- 케이스 코멘트에서 카카오 발송 이력, 상태 변경 이력 확인
- **포인트**: AI가 케이스를 분석하고 실제 행동(메시지 발송)까지 수행

### Scene 3 — 에이전트 메모리 (20초)

`/{org}/agents` → Orchestrator 클릭

- Memory 탭: 학생별 인사이트, 이탈 위험 순위 확인
```json
"lastInsight": "이번 달 이탈 위험 학생 3명 — 이수아 > 김서준 > 한지민"
```
- **포인트**: 에이전트가 학원 상황을 시간이 지남에 따라 기억하고 축적

### Scene 4 — 텔레그램 봇 (40초)

텔레그램에서 아래 메시지 전송:
```
이수아 학생 최근 출결 현황 알려줘
```
또는
```
이번 주 보강 필요한 학생 정리해줘
```

- 10초 내에 AI 응답이 텔레그램으로 도착
- `/{org}/cases`에서 방금 생성된 케이스 확인
- **포인트**: 채팅 하나로 케이스 등록 → AI 처리 → 채널 응답 완전 자동화

---

## 4. 텔레그램 봇

> 봇 링크: **심사 전 업데이트 예정**

**추천 메시지 예시:**
- `이수아 학생 상담 예약 잡아줘`
- `이번 달 환불 요청 현황 정리해줘`
- `다음 주 보강 일정 잡아야 할 학생 있어?`
- `수강료 미납 학생 명단 줘`

---

## 5. 카카오채널

> 카카오채널: `pf.kakao.com/_raDdX`

학부모처럼 채팅을 보내면 AI가 케이스를 생성하고 처리합니다.

---

## 6. 데모 데이터 구성 (탄자니아 영어학원)

| 데이터 | 수량 |
|--------|------|
| 학생 | 15명 (초등·중등·고등·성인부) |
| 강사 | 6명 |
| 수업 일정 | 32개 (정규·보강·상담·행사·법정 일정) |
| 케이스 | 29개 (AI 처리 이력 + 코멘트 포함) |
| 문서 | 5개 (환불 정책, 상담 정책, 학부모 FAQ 등) |
| 에이전트 | 5개 (orchestrator·complaint·scheduler·retention·notification) |

---

## 7. 심사 포인트별 대응

| 심사 항목 | 확인 위치 |
|---------|---------|
| **기술적 완성도** | `/cases` 처리 이력, Telegram 왕복, 케이스→승인→일정 side effect |
| **AI 활용 능력** | `/agents` → Memory 탭, `agentDraft` 응답, `.agent/system/` AI 기획 문서 |
| **기획력·실무 접합성** | `/students` 리스크 스코어, `/schedule` 법정 일정 포함, 이탈 위험 감지 |
| **창의성** | k-skill 레지스트리 (`/skills`), 에이전트 메모리 시스템, 멀티채널 통합 |

---

## 8. Fallback 안내

| 기능 | 상태 | Fallback |
|------|------|---------|
| AI 응답 | Anthropic Claude | `DEMO_MODE=true` → mock 응답 |
| 텔레그램 | 실제 봇 연동 | 라이브 URL에서 실제 동작 |
| 카카오채널 | webhook 연동 | 수동 케이스 생성으로 동일 플로우 |
| Google Calendar | 미연동 | 내부 일정 관리로 대체 |

---

## 9. Known Limits

- Google Calendar 실시간 동기화: 미구현 (내부 일정 관리로 대체)
- 카카오 아웃바운드: 실전 webhook URL 설정 필요
- 다중 사용자 동시 접속: 단일 세션 기준으로 최적화

---

*HagentOS — AI가 학원을 운영한다*
